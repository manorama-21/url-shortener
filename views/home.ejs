<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    
<style>
    body{
        background-color:rgb(161, 207, 197);
    }
    h2{
        text-align: center;
        font-size: 50px;
        
    }
    .center {
        margin: auto;
        width: 90%;
        border: 2px solid green;
        padding: 10px;
        text-align: center;
        font-size: 20px;
       
       
      }
      label{
        font-size: 25px;
      }
      table{
        width:100%;
        margin-left: auto;
  margin-right: auto;
  
      }
      button{
        background-color: rgb(173, 127, 94);
        font-size: 20px;
      }
      input{
        background-color: rgb(211, 183, 164);
        font-size: 20px;
      }
</style>
</head>
<body>
    <h2><u>URL Shortener</u></h2>

  
        <form action="/url" method="post">  
            <div class="center">
            <label>Enter an URL</label>
            <input type="text" name="url" placeholder="https://www.ermks.in"/>
            <button type="submit">Generate URL</button>
        </div>
        </form>

<br/><br/>
        <div class="center">
            <table border="1px">
                <thead>
                    <th>S.No</th>
                    <th>Short ID</th>
                    <th>GO TO</th>
                    <th>Total Click</th>
                    <th>Original URL</th>
                    <th>Operator</th>
                </thead>
                <% if (locals.urls){ %>
                <tbody>
                    <% urls.forEach((url,index)=> { %>
                    <tr>
                        <td>
                            <%= index + 1 %>
                        </td>

                        <td>
                            <%= url.ShortID %>
                        </td>

                        <td>
                           <a href ='http://localhost:3000/url/<%= url.ShortID %>' target ='_blank'>Click Here</a>
                        </td>

                        <td>
                            <%= url.VisitedHistory.length %>
                        </td>

                        <td>
                            <%= url.RedirectURL %>
                        </td>
                        <td><a href="/delete/<%= url._id %> >">Delete</a></td>
                    </tr>
                  <% }) %>
                </tbody>
           <% } %>
            </table>
        </div>

 
</body>
</html>